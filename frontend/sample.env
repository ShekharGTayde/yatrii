# Backend API base URL
# Keep /api/v1 and avoid trailing slash for clarity.
VITE_BASE_URL=http://localhost:3000/api/v1

# Optional explicit socket server URL
# If omitted, app derives socket URL from VITE_BASE_URL
VITE_SOCKET_URL=http://localhost:3000
